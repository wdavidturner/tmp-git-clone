{
  "name": "tmp-git-clone",
  "version": "1.0.0",
  "description": "Clone git repositories to /tmp using shallow clone",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "tmp-git-clone": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "git",
    "clone",
    "cli",
    "tmp",
    "shallow-clone"
  ],
  "license": "MIT",
  "dependencies": {
    "commander": "^13.0.0",
    "picocolors": "^1.1.1",
    "yocto-spinner": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
